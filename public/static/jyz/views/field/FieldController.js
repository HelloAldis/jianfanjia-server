(function() {
    angular.module('controllers')
        .controller('FieldController', [
            '$scope','$rootScope',
            function($scope, $rootScope) {
               console.log('（后续实现）');
            }
        ]);
})();
